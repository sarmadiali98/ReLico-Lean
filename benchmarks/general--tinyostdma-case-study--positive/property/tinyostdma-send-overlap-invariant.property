property {
    define {
        NoOverlappingSend = (sensorNodeSenderDevice.sendWhileBusy == false) &&
                            (receiver.sendWhileBusy == false);
    }

    Assertion {
        TinyOSTDMA_NoOverlappingSend : NoOverlappingSend;
    }
}
