reactiveclass Consumer(2) {
    msgsrv accept(int value) {
        sender.tick();
    }
}

reactiveclass Producer(3) {
    knownrebecs {
        Consumer sink;
    }
    msgsrv tick() {
        sink.accept(1);
    }
}

main {
    Producer producer0(consumer0):();
    Consumer consumer0():();
}
