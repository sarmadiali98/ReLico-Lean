reactiveclass TwoStateController(10) {
    statevars {
        int x;
        int y;
    }

    TwoStateController() {
        x = 1;
        y = 2;
        self.tick() after(1);
    }

    msgsrv tick() {
        y = x;
    }
}

main {
    TwoStateController controller():();
}
