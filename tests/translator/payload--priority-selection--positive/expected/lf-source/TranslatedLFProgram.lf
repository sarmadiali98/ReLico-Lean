target Cpp

reactor Solo {
  state x: int = 0
  logical action pick_action: void

  reaction(startup) -> pick_action {=
    x = 0;
    pick_action.schedule(1ms);
  =}

  reaction(pick_action) -> pick_action {=
    x = 1;
    pick_action.schedule(1ms);
  =}
}

main reactor {
  solo = new Solo()
}
