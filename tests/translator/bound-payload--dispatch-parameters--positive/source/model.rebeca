// Benchmark: bound-payload--dispatch-parameters--positive
// Formerly bound-payload--dispatch--negative, re-polarized: the restriction it encoded is
// obsolete under the current verified fragment. See README.
reactiveclass Controller(10) {
    statevars {
        int x;
    }

    Controller() {
        x = 0;
        self.dispatch(1) after(1);
    }

    msgsrv dispatch(int payload) {
        x = payload;
        self.dispatch(payload) after(1);
    }
}

main {
    Controller controller():();
}
